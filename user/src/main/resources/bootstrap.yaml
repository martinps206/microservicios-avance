spring:
  application:
    name: user
  datasource:
    url: jdbc:mysql://localhost:3306/microuser?useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: mysql
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  cloud:
    config:
      enabled: true
      uri: http://localhost:8081